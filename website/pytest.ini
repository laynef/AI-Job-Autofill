[pytest]
addopts = --cov=. --cov-report=term-missing --cov-fail-under=90
markers =
    unit: fast, isolated tests
    integration: exercises multiple parts of the app together
    build: build/smoke verification tests
    e2e: browser-driven end-to-end tests
    mobile: mobile simulator tests (Appium or device emulation)
